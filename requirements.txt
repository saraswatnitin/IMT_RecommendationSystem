Flask==3.0
scikit-learn==0.24.2
pandas==1.3.3  # replace "latest_version" with the actual latest version if known
numpy==1.21.2  # replace "latest_version" with the actual latest version if known
nltk==3.6.5  # replace "latest_version" with the actual latest version if known

